turtlebot3:
    # Stage 2: Static obstacles - Learn obstacle avoidance
    
    task_and_robot_environment_name: 'TurtleBot3World-v0'
    ros_ws_abspath: "/home/ubuntu/simulation_ws"
    launch_file_name: "stage2_static_obstacles.launch"  # Stage 2 world
    stage: 2
    
    # Environment parameters
    running_step: 0.04
    pos_step: 0.016
    max_episode_steps: 400  # More steps for navigation with obstacles
    
    # Robot control
    linear_forward_speed: 0.22
    linear_turn_speed: 0.1
    angular_speed: 1.5
    init_linear_forward_speed: 0.0
    init_linear_turn_speed: 0.0
    
    # Observations
    new_ranges: 5
    min_range: 0.15  # Tighter collision threshold
    max_laser_value: 3.5
    min_laser_value: 0.0
    n_actions: 3
    
    # Collision detection
<<<<<<< Updated upstream
    max_linear_aceleration: 1.2  # More sensitive
    
    # DQN parameters - Transfer learning from stage 1
    gamma: 0.995
    epsilon_start: 0.7  # Start with some exploitation from stage 1
    epsilon_end: 0.05
    epsilon_decay: 2000
    n_episodes: 1500
    target_update: 10
    batch_size: 128
    learning_rate: 0.0005  # Lower learning rate for fine-tuning
    
    # Checkpoint and Mode Settings - Load from stage 1
    load_pretrained_model: true  # Load model weights from stage 1
    checkpoint_file: "checkpoint_latest.pth"  # Load from stage 1 or "checkpoint_ep1000.pth"
    inference_mode: false  # Set to true for evaluation only (no training)
=======
    max_linear_aceleration: 1.2 # More sensitive

    # Arena boundaries (4x4 meter bounded box)
    arena_min_x: -2.0
    arena_max_x: 2.0
    arena_min_y: -2.0
    arena_max_y: 2.0

    goal_positions:
        # Safe goal marker positions (x, y) in the 4x4m arena, avoiding all obstacles
        - [-1.5, 0]
        - [0, -1.5]
        - [0, 0]
        - [0, 1.5]
        - [1.5, 0]
        - [-1.5, 1]
        - [1, 1.5]
        - [-1.5, -1]
        - [1, -1.5]
        - [-1, 1.5]
        - [-1, -1.5]
        - [1.5, -1]
        - [1.5, 1]
        - [-1.5, -0.5]
        - [-1.5, 0.5]
        - [1.5, -0.5]
        - [1.5, 0.5]
        - [-0.5, 1.5]
        - [0.5, 1.5]
        - [-0.5, -1.5]
        - [0.5, -1.5]
        - [0, -1]
        - [0, 1]
        - [-1, 0]
        - [1, 0]

    # Safe goal spawning boundaries
    safe_arena_min_x: -1.0
    safe_arena_max_x: 1.0
    safe_arena_min_y: -1.0
    safe_arena_max_y: 1.0
    min_spawn_distance: 0.5
    success_threshold: 0.25

    # DQN parameters - Fine-tuning from stage 1
    # Fine-Tuning Parameters
    gamma: 0.99
    epsilon_start: 0.2    # Low start: we trust the model
    epsilon_end: 0.01
    epsilon_decay: 5000   # Fast decay: stop exploring quickly
    n_episodes: 500       # Short run just to polish behavior
    batch_size: 128
    learning_rate: 0.000005 # Extremely low: just polish weights, don't break them
    tau: 0.005 
    replay_memory_size: 50000

    # Reward settings (Stricter for smooth driving)
    distance_reward_multiplier: 20.0
    turn_penalty_multiplier: 1.0  # High penalty: stop wiggling!
    yaw_reward_multiplier: 0.5    
    time_penalty: -0.1
    goal_reached_reward: 200
    obstacle_hit_penalty: -200
    courage_zone_threshold: 0.5

    # Checkpoint and Mode Settings 
    load_pretrained_model: true
    checkpoint_file: 'stage_2_20260208-225334/models/checkpoint_model_stage2_20260209_104437.pth'
    inference_mode: true
>>>>>>> Stashed changes
